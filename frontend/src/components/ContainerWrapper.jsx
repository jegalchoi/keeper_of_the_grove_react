import styled from 'styled-components'

export const ContainerWrapper = styled.div`
  .container {
    display: 'flex',
    flexWrap: 'wrap',
    position: 'absolute',
    top: 0,
    left: 0,
    height: '100%',
    width: '100%',
    alignItems: 'center'
  }
`
